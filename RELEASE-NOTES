Release Notes for IRRToolSet version 4.7.2
***************************************************

This release is intended to be a bug fix release, as well as introducing
some updates.

rpsl:

o Dictionary updated - "descr" turned to multiple

RtConfig:

o fixed a bug in AS_PATH filter generation for cisco and junos configurations

prpath:

o filter-set names resolving added

Aoe is alive now, several bugfixes and changes made:

o bugs fixed; all documented features are in place
o peer list can be saved to a file
o syntax error reporting improved

